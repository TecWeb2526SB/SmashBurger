/**
 * Logica JavaScript globale per l'applicazione SmashBurger.
 * Gestisce l'interattività comune a tutte le pagine.
 */

document.addEventListener('DOMContentLoaded', () => {
    // Placeholder per logica futura (es. gestione menu, animazioni errori, ecc.)
    console.log('SmashBurger app initialized.');
});
